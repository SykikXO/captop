# Worker

Cloudflare Worker deployment files.

- **cloudflare_worker.js**: Proxy script to route requests between users and the backend server.
- **wrangler.toml**: Configuration for Wrangler deployment.
